# Uses postgresql

development:
  adapter: postgresql
  encoding: utf8
  database: collisterator_development
  pool: 5
  username: collisterator
  password: guest
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  encoding: utf8
  database: collisterator_test
  pool: 5
  timeout: 5000

production:
  adapter: postgresql
  encoding: utf8
  database: collisterator_production
  pool: 5
  timeout: 5000
